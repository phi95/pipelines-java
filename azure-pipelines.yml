resources:
  repositories:
  - repository: template-folder  # identifier (A-Z, a-z, 0-9, and underscore)
    type: github  # see the following "Type" topic
    name: phi95/azure-pipelines  # repository name (format depends on `type`)

extends:
  template: stages-template.yml@template-folder
  parameters:
    name: 'hello'
